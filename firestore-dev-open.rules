rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: Open access for all authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}